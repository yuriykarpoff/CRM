'use strict';

(() => {
    const FIGURES_ENG = ['rock', 'sissors', 'paper'];
    const FIGURES_RUS = ['камень', 'ножницы', 'бумага'];
})();

const game = (language) => {
    const result = {
        player: 0,
        computer: 0,
    };

    return function start() {

    };

    window.RPS = game;
};